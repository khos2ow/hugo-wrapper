name: check

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  Check:
    name: ShellCheck
    runs-on: ubuntu-latest
    steps:
    - name: Check out code into directory
      uses: actions/checkout@v1

    - name: Run ShellCheck
      run: make check
